import { Message } from '@sap/artifact-management-base';
export default class MessageUtil {
    static addMessageTo(target: {
        messages?: Message[];
    }, message: Error | Message): void;
    static messageFromError(error: Error): Message;
}
