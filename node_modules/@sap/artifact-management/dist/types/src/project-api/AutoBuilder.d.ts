/// <reference types="node" />
import { EventEmitter } from 'events';
import { ProjectFileSystem, FileSystemWatchManager, IProjectInstance } from '@sap/artifact-management-base';
declare class AutoBuilder extends EventEmitter {
    private fs;
    private fileSystemWatchManager;
    private project;
    private moduleAutoBuilder;
    private callUpdateModules;
    constructor(fs: ProjectFileSystem, fileSystemWatchManager: FileSystemWatchManager, project: IProjectInstance);
    start(): Promise<void>;
    destroy(): Promise<void>;
    updateModules(): Promise<void>;
}
export default AutoBuilder;
