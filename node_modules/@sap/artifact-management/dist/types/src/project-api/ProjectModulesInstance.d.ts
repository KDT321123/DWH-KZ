import { ProjectFileSystem, ProjectData, Project, ItemFilter, Item, ModuleData, FileSystemWatchManager, ItemType, KeyIn, ItemDetailInfoOptions } from '@sap/artifact-management-base';
import ModuleInstance from './ModuleInstance';
import ProjectEntityInstance from './ProjectEntityInstance';
/**
 * Module directories of the project
 */
export default class ProjectModulesInstance extends ProjectEntityInstance<string[]> {
    readonly projectData: ProjectData;
    private readModuleAsProject;
    constructor(watchManager: FileSystemWatchManager, fs: ProjectFileSystem, projectData: ProjectData, readModuleAsProject?: boolean);
    loadModuleDirectories(): Promise<{
        data: string[];
        watchPatterns: string[];
    }>;
    loadModuleInstances(directoryPaths: string[]): Promise<ModuleInstance[]>;
    getModules(): Promise<ModuleData[]>;
    readDeep(): Promise<Project | undefined>;
    getModuleDirectories(): Promise<string[]>;
    getModuleInstances(): Promise<ModuleInstance[]>;
    readItems(filter?: ItemFilter): Promise<Item[]>;
    private getItemsFilteredByTags;
    detailInfo(type: KeyIn<typeof ItemType>, ref: string, entityPath?: string, options?: ItemDetailInfoOptions): Promise<Item | undefined>;
}
