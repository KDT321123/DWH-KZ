"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ajv_1 = __importDefault(require("ajv"));
const artifact_management_base_1 = require("@sap/artifact-management-base");
const ajv = new ajv_1.default();
async function run() {
    const fs = artifact_management_base_1.FileSystemFactory.getFileSystem(__dirname);
    const schema = await fs.readJson('project-properties-schema.json');
    delete schema.$schema;
    const validate = ajv.compile(schema);
    const data = { 'namespacex': 'abc' };
    if (validate(data)) {
        console.log('ok');
    }
    else {
        console.log(validate.errors);
    }
}
run();
//# sourceMappingURL=SchemaTest.js.map