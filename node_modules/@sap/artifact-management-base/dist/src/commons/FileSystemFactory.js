"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.FileSystemFactory = void 0;
const LoggerManager_1 = require("../logging/LoggerManager");
class FileSystemFactory {
    // private static getVscode() {
    //     try {
    //         return require('vscode');
    //     } catch (error) {
    //         return undefined;
    //     }
    // }
    static init() {
        if (!FileSystemFactory.initialised) {
            FileSystemFactory.isInVscode = false; //!!FileSystemFactory.getVscode();
            // if (FileSystemFactory.isInVscode) {
            //     //getLogger().info('> Will be using Vscode FS');
            // } else {
            LoggerManager_1.getLogger().info('> Will be using Node FS');
            // }
            this.initialised = true;
        }
    }
    static getFileSystem(basePath, rootPath) {
        FileSystemFactory.init();
        // let FileSystemClass;
        // if (FileSystemFactory.isInVscode) {
        //     //FileSystemClass = require('./VSCodeFileSystem').VSCodeFileSystem;
        // } else {
        const FileSystemClass = require('./NodeFileSystem').NodeFileSystem;
        // }
        return new FileSystemClass(basePath, rootPath);
    }
}
exports.FileSystemFactory = FileSystemFactory;
FileSystemFactory.initialised = false;
FileSystemFactory.isInVscode = false;
//# sourceMappingURL=FileSystemFactory.js.map