import { KeyIn } from '../util/Types';
import { ProjectType } from '../definitions/ProjectType';
import { ModuleReader } from './ModuleReader';
import { DetectionMechanism } from './PluginEntryTypes';
import ProjectData from './ProjectData';
import { ProjectReader, ProjectReaderOptions } from './ProjectReader';
export default class ProjectWrapper extends ProjectReader {
    private moduleReader;
    constructor(moduleReader: ModuleReader);
    getType(): KeyIn<typeof ProjectType>;
    getDetectionMechanism(): DetectionMechanism;
    tags: string[];
    read({ fs }: ProjectReaderOptions): Promise<ProjectData | undefined>;
}
