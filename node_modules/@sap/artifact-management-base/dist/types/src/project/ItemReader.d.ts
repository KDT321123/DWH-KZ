import Item from './Item';
import ProjectEntityReader from '../project-api/ProjectEntityReader';
import ProjectFileSystem from '../commons/ProjectFileSystem';
export declare type ItemDetailInfoOptions = {
    inherited?: boolean;
};
/**
 * Reader for entities of one or more types
 */
export default interface ItemReader extends ProjectEntityReader<Item[]> {
    /**
     * Super set of all item types that the reader may return
     */
    types: string[];
    detailInfo?(fsPath: ProjectFileSystem, ref: string, options?: ItemDetailInfoOptions): Promise<Item | undefined>;
}
