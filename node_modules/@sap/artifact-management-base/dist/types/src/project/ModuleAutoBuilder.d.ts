import ProjectFileSystem from '../commons/ProjectFileSystem';
import IProjectInstance from './IProjectInstance';
import { KeyIn } from '../util/Types';
import { ModuleType } from '../definitions/ModuleType';
/**
 * Automatically build files
 *
 */
export default class ModuleAutoBuilder {
    getType(): KeyIn<typeof ModuleType>;
    patterns(): string[];
    runInitial(fs: ProjectFileSystem, project: IProjectInstance): Promise<void>;
    runUpdate(fs: ProjectFileSystem, project: IProjectInstance): Promise<void>;
}
