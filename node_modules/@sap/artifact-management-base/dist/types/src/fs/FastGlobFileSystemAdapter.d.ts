export declare type FileFilterFunction = (path: string, isDir: boolean) => boolean;
export default class FastGlobFileSystemAdapter {
    readdir: any;
    constructor(allowFilter: FileFilterFunction);
}
