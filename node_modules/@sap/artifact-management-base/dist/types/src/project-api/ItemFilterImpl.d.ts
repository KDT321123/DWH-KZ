import ItemFilter from '../project/ItemFilter';
import Item from '../project/Item';
import ItemFilterInterface from './ItemFilterInterface';
declare function matchAlways(value: string): boolean;
declare function matchAlwaysArray(values: string[]): boolean;
export default class ItemFilterImpl implements ItemFilterInterface {
    matchTags: typeof matchAlwaysArray;
    matchType: typeof matchAlways;
    matchRef: typeof matchAlways;
    constructor(filter: ItemFilter);
    match(entity: Item): boolean;
}
export {};
