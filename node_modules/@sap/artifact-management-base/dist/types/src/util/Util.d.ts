/// <reference types="node" />
import { SpawnOptions } from 'child_process';
export declare type ExecResult = {
    stdout?: string;
    stderr?: string;
};
export default class Util {
    static exec(command: string, ...args: string[]): Promise<ExecResult>;
    static exec(options: SpawnOptions, command: string, ...args: string[]): Promise<ExecResult>;
    static execute(command: string, path: string): Promise<{
        stdout: any;
        stderr: any;
    }>;
    static commandLineExecutor(command: any): Promise<any>;
}
