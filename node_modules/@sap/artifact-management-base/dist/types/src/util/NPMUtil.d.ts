declare class NPMUtilClass {
    private globalRootPathCache?;
    private cdsPathCached?;
    globalRootPath(): Promise<string | undefined>;
    getCDSPath(): Promise<string | undefined>;
    globalModulePath(name: string): Promise<string | undefined>;
    assertCdsMinVersion(cds: any): boolean;
    checkMTA(): Promise<boolean>;
    checkLocalPackage(packageName: string): boolean;
}
declare const NPMUtil: NPMUtilClass;
export default NPMUtil;
