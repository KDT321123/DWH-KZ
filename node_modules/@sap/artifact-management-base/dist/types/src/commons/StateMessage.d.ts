export interface StateMessage {
    code: string;
    description: string;
}
export declare class Info {
    private stateMessages;
    private static ModuleCode;
    add(messageOrError: {
        code: string;
        description: string;
    } | Error, moduleType?: string): Info;
    getStateMessages(): {} | undefined;
}
