sap.ui.define(['sap/fe/test/ListReport'], function(ListReport) {
    'use strict';

    var AdditionalCustomListReportDefinition = {
        actions: {},
        assertions: {}
    };

    return new ListReport(
        {
            appId: '<%- app.id %>',
            componentId: '<%- template.settings.entityConfig.mainEntityName %>List',
            entitySet: '<%-  template.settings.entityConfig.mainEntityName %>'
        },
        AdditionalCustomListReportDefinition
    );
});