sap.ui.require(
    [
        'sap/fe/test/JourneyRunner',
        '<%- app.id.replace(/\./g, "/") %>/test/integration/pages/MainListReport' ,
        '<%- app.id.replace(/\./g, "/") %>/test/integration/pages/MainObjectPage',
        '<%- app.id.replace(/\./g, "/") %>/test/integration/OpaJourney'
    ],
    function(JourneyRunner, MainListReport, MainObjectPage, Journey) {
        'use strict';
        var JourneyRunner = new JourneyRunner({
            // start index.html in web folder
            launchUrl: sap.ui.require.toUrl('<%- app.id.replace(/\./g, "/") %>') + '/index.html'
        });

        JourneyRunner.run(
            {
                pages: { <% if (template.type !== 'feop') { %>onTheMainPage: MainListReport,<% } %> onTheDetailPage: MainObjectPage }
            },
            Journey.run
        );
    }
);