sap.ui.define(['sap/fe/test/ObjectPage'], function(ObjectPage) {
    'use strict';

    // OPTIONAL
    var AdditionalCustomObjectPageDefinition = {
        actions: {},
        assertions: {}
    };

    return new ObjectPage(
        {
            appId: '<%- app.id %>', // MANDATORY: Compare sap.app.id in manifest.json
            componentId: '<%-  template.settings.entityConfig.mainEntityName %>ObjectPage', // MANDATORY: Compare sap.ui5.routing.targets.id in manifest.json
            entitySet: '<%-  template.settings.entityConfig.mainEntityName %>' // MANDATORY: Compare entityset in manifest.json
        },
        AdditionalCustomObjectPageDefinition
    );
});