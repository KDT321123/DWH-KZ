import type { Floorplan } from '@sap/ux-generator-common';
import type { Answers, Question } from 'yeoman-generator';
/**
 * Determine the target folder using the following :
 * 1) Use the App Wizard target folder config
 * 2) Use the 1st mounted folder if no active workspace
 * 3) Use the users home dir
 *
 * @param vscode
 * @returns The default path, if it can be determined otherwise undefined.
 */
export declare function getDefaultTargetFolder(vscode: any): string;
/**
 * Get the prompts for the generator type (application type)
 * @param rootGenVersion, the version to appear in the prompt hint
 * @param floorplans, floorplans which will be prompted
 * @returns
 */
export declare function getGeneratorTypePrompts(genVersion: string, floorplans: Floorplan[]): Question<Answers>;
export declare function getFloorplan(answers: Answers): Floorplan;
//# sourceMappingURL=utils.d.ts.map