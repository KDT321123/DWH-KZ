import { IProjectInstance, Item, ItemFilter, ModuleData } from '@sap/artifact-management-base';
export declare class ProjectInstanceMock implements IProjectInstance {
    readItems(filter?: ItemFilter): Promise<Item[]>;
    readModules(): Promise<ModuleData[]>;
}
