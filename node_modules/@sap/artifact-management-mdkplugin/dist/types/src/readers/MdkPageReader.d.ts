import { Item } from '@sap/artifact-management-base';
import MdkItemReader from './MdkItemReader';
import MdkPage from '../MdkPage';
export declare const MDK_PAGE_PATTERNS: string[];
export default class MdkPageReader extends MdkItemReader {
    defaultType: string;
    types: string[];
    tags: string[];
    matchConditions: {
        requiredFilePatterns: string[];
    };
    protected itemParser(page: MdkPage, idx: number): Promise<Item>;
}
