import { ProjectFileSystem } from '@sap/artifact-management-base';
import MdkItemReader from './MdkItemReader';
export declare const MDK_RULE_PATTERNS: string[];
export default class MdkRuleReader extends MdkItemReader {
    defaultType: string;
    types: string[];
    tags: string[];
    matchConditions: {
        requiredFilePatterns: string[];
    };
    protected readEntities(paths: string[], fs: ProjectFileSystem): Promise<string[]>;
}
