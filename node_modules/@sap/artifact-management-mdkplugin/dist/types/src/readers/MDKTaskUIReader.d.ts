import MdkItemReader from './MdkItemReader';
import { ProjectFileSystem } from '@sap/artifact-management-base';
export declare const MDK_TaskUI_PATTERNS: string[];
export default class MDKTaskUIReader extends MdkItemReader {
    defaultType: string;
    types: string[];
    tags: string[];
    matchConditions: {
        requiredFilePatterns: string[];
        rule: (context: {
            matchType: 'required' | 'mustNotExist';
            fs: ProjectFileSystem;
        }) => Promise<boolean>;
    };
    protected itemParser(app: any, idx: number): Promise<{
        type: string;
        ref: string;
        name: string;
        path: string;
        tags: string[];
    }>;
}
