import { ModuleAutoBuilder, IProjectInstance, ProjectFileSystem, ModuleType, KeyIn } from '@sap/artifact-management-base';
export default class MdkServiceBuild extends ModuleAutoBuilder {
    getType(): KeyIn<typeof ModuleType>;
    patterns(): string[];
    runInitial(fs: ProjectFileSystem, project: IProjectInstance): Promise<void>;
    runUpdate(fs: ProjectFileSystem, project: IProjectInstance): Promise<void>;
    rebuild(fs: ProjectFileSystem, project: IProjectInstance): Promise<void>;
}
