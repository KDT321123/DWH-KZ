import { KeyIn, RunUI, RunUIApplication, RunUIOptions, ModuleType } from '@sap/artifact-management-base';
export default class MdkRunUI implements RunUI {
    getType(): KeyIn<typeof ModuleType>;
    applications({ fs, module }: RunUIOptions): Promise<RunUIApplication[]>;
}
